<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Homepage</title>
    <style>
       body {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
        padding: 0;
        font-family:Cambria, Cochin, Georgia, Times, 'Times New Roman', serif;
       }
        nav {
            background-color: #15639f;
            overflow: hidden;
            width: 100%;
            position: fixed;
            top: 0;
            left: 0;
            z-index: 0;
        }
        nav a {
            float: left;
            color: white;
            text-align: center;
            padding: 14px 20px;
            text-decoration: none;
        }
        nav a:hover {
            background-color: #ddd;
            color: black;
        }

        .summary{
            background-color: #f0f0f0;
            padding: 20px;
            border-radius: 5px;
            text-align: center;
            margin-top: 60px;
        }

    </style>
</head>
<body>
    <nav>
        <a href="/">Homepage</a>
        <% if(!id){%>
        <a href="/users/login">Login</a>
        <a href="/users/new">Signup</a>
        <%}else{%>
        <a href="/blueprints/start">Create BluePrint</a>
        <a href="/blueprints/">My BluePrint</a>
        <a href="/users/getSkills">Add your experience</a>
        <a href="/users/logout">Log Out</a>
        <%}%>
    </nav>
</body>
</html>